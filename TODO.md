# MaMa — Project TODO / Roadmap

Живой список задач. Отмечай `[x]` по мере готовности. Обновлено: 2026-07-07.

---

## ✅ Сделано (MVP-фундамент)

- [x] Монорепо-скаффолд: `app/` (Flutter) · `api/` · `worker/` · `migrations/`
- [x] Docker Compose (db, redis, migrate, api, worker) + `.env.example`
- [x] CI на GitHub Actions (flutter analyze/test + api/worker typecheck) + бейдж
- [x] БД: users, children_profiles, pregnancies, medical_protocols, scheduled_alerts,
      doctor_patient_links, patient_locations + **partial-индексы** + seed протоколов
- [x] API: `/health`, `/profiles`, `/pregnancies/:id/birth` (транзакционно), `/patients/:id/geo`
- [x] **JWT-auth**: `/auth/register`, `/auth/login` (scrypt) + seed-скрипт демо-данных
- [x] BOLA/IDOR-guard для гео (doctor↔patient + district)
- [x] Worker: идемпотентный ежедневный планировщик уведомлений (cron + run-once)
- [x] Flutter BLoC: ProfileSwitchBloc, BirthTriggerBloc, GrowthCubit
- [x] Экран беременности: **календарь по неделям** (фрукт-размер, вес, инфо-блоки
      «О малыше/О Вас/Рекомендуем») + **анимированный виртуальный плод**
- [x] Трекер шевелений (пульсирующее сердце, haptic)
- [x] Триггер «Я родила!» + форма новорождённого (валидация)
- [x] Режим ребёнка: **график роста ВОЗ** (вес-к-возрасту, P3/P50/P97) + добавление замеров
- [x] Переключение профилей (беременность ↔ дети)
- [x] Запуск на Android-эмуляторе подтверждён скриншотами

---

## ✅ Full-stack и деливераблы ТЗ (реализовано + проверено)

- [x] **Бэкенд end-to-end живой**: Node.js + PostgreSQL 16 (без Docker), `npm run migrate`
      (идемпотентный, `schema_migrations`), `npm run seed`, API на :8080
- [x] Клиент↔API: авто-логин (`AuthGateway`) + `--dart-define=USE_API=true` → реальные профили из БД
- [x] **Birth Trigger через API** проверен в БД: ребёнок создан, беременность архивирована,
      расписание прививок сгенерировано (БЦЖ+гепатит B)
- [x] **Телемедицина**: WebRTC-сигналинг (`/ws/signal`, JWT + комнаты) + `POST /patients/me/geo`
- [x] **Data Engineer**: медальонная аналитика Bronze→Silver→Gold (`0004_analytics.sql`,
      витрины `gold_screening_completion`/`week12`/`daily_active_users`)
- [x] **DevOps**: Prometheus `/metrics` + K8s-манифесты (infra/app/monitoring) + prometheus.yml
- [x] **MaMa AI реальный Claude** (`claude-opus-4-8` через `@anthropic-ai/sdk`, env-gated,
      локальный фолбэк без ключа) — `POST /assistant/chat`
- [x] Редизайн UI: свежая пастель + сочные градиенты, крупные цифры (все вкладки)
- [x] Календарь развития ребёнка (навыки/прививки/совет + график ВОЗ)
- [x] 12 рабочих сервисов (счётчик схваток 5-1-1, монитор веса, чеклист, имена, группа крови, УЗИ, …)
- [x] Видео-лента (карточки), конфетти при рождении

**Осталось для продакшена:** реальный `ANTHROPIC_API_KEY` для Claude · вынос scheduler в `@mama/shared` ·
перенос проекта из OneDrive · Data-gold в админку/дашборд · WebRTC-клиент во Flutter.

---

## ✅ Сессия 2026-07-04…07 — движок вовлечения, трекинг, цикл, UX (всё с бэкендом, проверено curl)

Позиционирование уточнено: **ассистент + напоминания + трекинг данных**, НЕ контент (см. память проекта).

- [x] **Engagement API** (`0005`): `/me` (профиль/баллы/стрик), ежедневный чек-ин (CTE), инбокс `/reminders`, вес `/me/weight`
- [x] Живой **Профиль** из `/me`, тост-награда за стрик + **бейдж** непрочитанных на колокольчике
- [x] **Видео** → открывает YouTube (`url_launcher` + `<queries>` в манифесте)
- [x] Персистентные трекеры: **шевеления** (`0006`), **схватки** (`0007`) — сессии + история
- [x] **Дневник самочувствия** (`0008`): настроение + симптомы + заметка, upsert по дню + **вода** (`0013`)
- [x] **Отчёт для врача** — агрегирует вес/шевеления/схватки/самочувствие/цикл → копирование
- [x] Персист **чеклистов** (`0009`) и **истории MaMa AI** (`0010`)
- [x] **Дневник малыша** (`0011`): кормления/сон/подгузники + сводка дня (детская сторона USP)
- [x] **Календарь прививок** ребёнка (нац. календарь РК, отметка по возрасту)
- [x] **«Мои тренды»** — график настроения, частые симптомы, динамика веса, активность малыша
- [x] **Онбординг**: задать ПДР (`POST /me/pregnancy`) → неделя пересчитывается
- [x] **Трекинг цикла** (`0012`, Flo-style): прогноз месячных, фертильное окно/овуляция, календарь фаз, ПМС-паттерны
- [x] **Проактивный ассистент**: `/me/insights` (цикл/симптомы/стрик/вода) → лента подсказок в чате
- [x] Цикл → **напоминания** в инбоксе; цикл + вода → подсказки ассистента
- [x] **QA**: таймзона (`.toLocal()` для TIMESTAMPTZ), гонка истории чата, фикс `setState(()=>future)` ×3
- [x] **Skill `ui-ux-pro-max`** в `.claude/skills/` + применён: контраст ≥4.5:1, `Semantics`/tooltips,
      семантические токены (дедуп hex↔токены), скринридер-описания графиков, error-state с «Повторить»

**Осталось (требует внешнего):** FCM-пуши (Firebase) · WebRTC-звонок (нативная сборка) ·
верификация всего на эмуляторе.

---

## Приоритет 1 — Навигационный каркас и профиль (по референсам) ✅

- [x] **Нижняя навигация** (5 вкладок): Главная · Мониторинг · MaMa AI · Видео · Профиль
- [x] Экран **«Профиль»**: аватар, город, имя, статус («Я мама»), карточка «Мои дети»
- [x] **Геймификация**: уровень, баллы (базово, статичные значения)
- [x] **Реферальная программа**: «Пригласить», бонусы (UI)
- [x] Разделы «Мои посты / Мои заказы / Мои вопросы» (UI)
- [x] Экран **«Сервисы»** (сетка плиток) + заглушки MaMa AI / Видео
- [x] Доработать: реальные данные профиля из API, навигация плиток к реальным сервисам ✅

---

## 🚧 Приоритет 2 — Сервисы (из референса «Сервисы»)

- [x] **Счётчик схваток** (длительность/интервал, правило 5-1-1 «пора в роддом») ✅
- [x] **Монитор веса мамы** (график прибавки + рекомендуемый коридор, добавление замеров) ✅
- [x] **Чеклисты / Подготовка к роддому** (секции, чекбоксы, прогресс) ✅
- [x] **Счётчик толчков** — отдельный экран-сервис ✅
- [x] Выбор имени для ребёнка (каталог + поиск + фильтр по полу/значению) ✅
- [x] Расшифровка УЗИ (справочник аббревиатур/показателей) ✅
- [x] Группа крови младенца (калькулятор ABO по родителям + резус-конфликт) ✅
- [x] «Что можно есть» / «Что можно делать» (списки «можно/избегать») ✅
- [x] Плитки «Календарь беременности/развития» открывают готовые экраны ✅

---

## 🚧 Приоритет 3 — Контент и AI

- [x] **MaMa AI**: рабочий чат-UI (пузыри, ввод, состояние) с локальным ассистентом
      на правилах + дисклеймер. Замена на Claude API = заменить `LocalAssistantService`
      на вызов backend `/assistant/chat` (интерфейс `AssistantService` уже готов). ✅
- [ ] Подключить реальный Claude API через backend (нужен ANTHROPIC_API_KEY)
- [x] **Видео**: лента роликов (карточки) → открывают YouTube по теме ✅
- [x] Экран уведомлений (колокольчик) — инбокс напоминаний + бейдж ✅ *(поиск по контенту — ещё нет)*
- [x] Ежедневные советы + **Daily Streak** (чек-ин, баллы, тост-награда, проактивные подсказки) ✅

---

## 🚧 Приоритет 4 — Телемедицина и гео

- [ ] WebRTC видеоконсультации врач↔пациент (сигналинг-сервер)
- [ ] Чат с врачом (WebSocket)
- [ ] Батареесберегающий гео-трекинг (geofence вместо постоянного GPS)
- [ ] Авто-уведомление участкового врача при смене района/города

---

## 🚧 Приоритет 5 — Данные, инфраструктура, качество

- [ ] Data Engineer: медальонная аналитика Bronze → Silver → Gold (dbt)
- [ ] Админка/дашборд для клиник (прохождение скринингов по районам)
- [ ] DevOps: Kubernetes-манифесты, Prometheus + Grafana (мониторинг WS)
- [ ] Вынести scheduler в общий пакет `@mama/shared` (npm workspaces) — устранить дублирование
- [ ] CI: сборка APK-артефакта + `package-lock.json` для воспроизводимых сборок
- [ ] Больше тестов (bloc/cubit, репозитории, API-эндпоинты)
- [ ] Прод-хранилище токена (secure storage без нативного jni, либо Kotlin-имплементация)

---

## 🐞 Технический долг / заметки

- [x] ~~Баг «клавиатура не работает» в форме рождения~~ — **диагностировано: НЕ баг приложения.**
      Ввод в поле работает (проверено `adb input text`). Причина — глюк рендеринга Gboard на
      эмуляторе при аппаратном GPU. Фикс: эмулятор с `-gpu swiftshader_indirect`, Cold Boot AVD,
      или тест на реальном телефоне. Код менять не нужно.
- [ ] **Перенести проект из OneDrive** (`C:\src\MaMaHealth`) — OneDrive лочит `build/` и ломает сборки
- [ ] Тема при переключении на ребёнка морфит фон, но карточки таймлайна захардкожены розовым — привязать к теме
- [ ] Таймлайн: активная (подсвеченная) карточка иногда не совпадает с центром PageView — выверить
- [ ] Форма рождения — привязать вес/рост к первому замеру в графике роста
- [ ] `/profiles` end-to-end против живого API (сейчас клиент по умолчанию in-memory)
- [ ] Lottie-конфетти для Birth-анимации (ассет `assets/lottie/confetti.json`)

---

## 🔧 Как запустить (памятка)

```powershell
# Эмулятор с программным GPU (иначе глючит экранная клавиатура):
& "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe" -avd mama_pixel -gpu swiftshader_indirect

# Flutter-клиент на эмуляторе (OneDrive должен быть закрыт!)
cd C:\Users\yeren\OneDrive\Desktop\Projects\MaMa\app
flutter run -d emulator-5554

# Бэкенд
cd ..; docker compose up -d db redis; docker compose run --rm migrate
docker compose up api worker
```

Репозиторий: https://github.com/yerenkanat/MaMaHealth
