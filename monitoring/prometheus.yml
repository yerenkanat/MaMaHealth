global:
  scrape_interval: 15s

scrape_configs:
  - job_name: mama-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"] # docker-compose service; в k8s — через service discovery
